<Window x:Class="BarangayCIS.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:BarangayCIS.Desktop.Converters"
        mc:Ignorable="d"
        Title="Barangay CIS - Login" Height="548" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20">
            <TextBlock Text="Barangay Citizen Information System" 
                      FontSize="24" FontWeight="Bold" 
                      Foreground="White" 
                      HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Login Form -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="40">
            <TextBlock Text="Login" FontSize="28" FontWeight="Bold" 
                      Margin="0,0,0,30" HorizontalAlignment="Center"/>
            
            <!-- Username -->
            <TextBlock Text="Username" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                    Height="40" Margin="0,0,0,20" 
                    Padding="10" FontSize="14"/>
            
            <!-- Password -->
            <TextBlock Text="Password" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <PasswordBox x:Name="PasswordBox" 
                        Height="40" Margin="0,0,0,20" 
                        Padding="10" FontSize="14"
                        PasswordChanged="PasswordBox_PasswordChanged"/>
            
            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}" 
                      Foreground="Red" 
                      Margin="0,0,0,20"
                      TextWrapping="Wrap"
                      Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            
            <!-- Login Button -->
            <Button Content="Login" 
                   Command="{Binding LoginCommand}"
                   Height="45" 
                   Background="#2196F3" 
                   Foreground="White"
                   FontSize="16" FontWeight="SemiBold"
                   BorderThickness="0"
                   Cursor="Hand"
                   IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                           CornerRadius="5" 
                                           Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                         VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            
            <!-- Loading Indicator -->
            <TextBlock Text="Logging in..." 
                      HorizontalAlignment="Center" 
                      Margin="0,10,0,0"
                      Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</Window>
